#! /usr/bin/env python
# encoding: utf-8

def build(bld):
  bld(features = 'cxx cxxstlib',
      source = [ 'SequenceFileLexer.l', 'SequenceFileParser.yc',
                 'SequenceFileScanner.cc', 'SequenceFileDriver.cc' ],
      target = 'sequence_parser',
      includes = '.',
      export_includes = '.',
      use='project-headers')
